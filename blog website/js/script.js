document.addEventListener('DOMContentLoaded', function () {
    // Select the .content element
    const content = document.querySelector('.content');
    
    // Add class 'active' to trigger animation
    content.classList.add('active');
});
